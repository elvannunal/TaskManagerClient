import { UserTask } from "./userTask";
import { User } from "./user";

export class CreateTeam {
    teamId?:number;
    teamName: string = '';
    //teamLead: User | null = null; 
   // members: User[] = []; 
   // task: UserTask[]=[];
}